<UserControl x:Class="RestaurantPOS.Views.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource PageBackground}"
             Foreground="{DynamicResource TextBlack}">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">


            <StackPanel HorizontalAlignment="Center">
                <Button Content="🛒 Point of Sale" 
                        Style="{StaticResource PrimaryButton}"
                        Width="300" 
                        Height="100" 
                        FontSize="24"
                        Command="{Binding GoToSellingCommand}"
                        Margin="20"/>
                
                <Button Content="⚙️ Backoffice" 
                        Style="{StaticResource PrimaryButton}"
                        Width="300" 
                        Height="100" 
                        FontSize="24"
                        Command="{Binding GoToBackofficeCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
