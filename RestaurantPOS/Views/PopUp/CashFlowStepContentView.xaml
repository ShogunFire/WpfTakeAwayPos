<UserControl x:Class="RestaurantPOS.Views.CashFlowStepContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:RestaurantPOS.Views.Controls"
             xmlns:views="clr-namespace:RestaurantPOS.Views">
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  Padding="0">
        <Grid>
            <!-- Step 0: Amount -->
            <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding DataContext.CurrentStepIndex, RelativeSource={RelativeSource AncestorType=views:StepPopupView}}" Value="0">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>

                <controls:KeypadControl UnitLabel="$"
                                        IsPrefix="True"
                                        Value="{Binding Amount, Mode=TwoWay}"/>
            </StackPanel>

            <!-- Step 1: Main Reason -->
            <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding DataContext.CurrentStepIndex, RelativeSource={RelativeSource AncestorType=views:StepPopupView}}" Value="1">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>

                <TextBlock Text="Select Category"
                           FontSize="16"
                           FontWeight="Bold"
                           Margin="0,0,0,12"
                           Foreground="{DynamicResource TextBlack}"/>
                <ListBox ItemsSource="{Binding Categories}"
                         SelectedItem="{Binding SelectedCategory}"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                </ListBox>
            </StackPanel>

            <!-- Step 2: Detailed Reason -->
            <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding DataContext.CurrentStepIndex, RelativeSource={RelativeSource AncestorType=views:StepPopupView}}" Value="2">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>

                <TextBlock Text="Select Reason"
                           FontSize="16"
                           FontWeight="Bold"
                           Margin="0,0,0,12"
                           Foreground="{DynamicResource TextBlack}"/>
                <ListBox ItemsSource="{Binding AvailableReasons}"
                         SelectedItem="{Binding SelectedReason}"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         HorizontalContentAlignment="Stretch">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,8" >
                                <TextBlock Text="{Binding Reason}"
                                           FontWeight="Bold"
                                           FontSize="14"
                                           Foreground="{DynamicResource TextBlack}"/>
                                <TextBlock Text="{Binding Description}"
                                           FontSize="12"
                                           Foreground="{DynamicResource TextDarkGray}"
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
