<UserControl x:Class="RestaurantPOS.Views.AddCashView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:RestaurantPOS.Views"
             xmlns:controls="clr-namespace:RestaurantPOS.Views.Controls"
             Background="{DynamicResource PageBackground}"
             Foreground="{DynamicResource TextBlack}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Navigation Menu -->
        <local:BackofficeNavMenu Grid.Column="0"/>

        <!-- Right Content Area -->
        <Border Grid.Column="1" Padding="40">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="600">
                <!-- Header -->
                <TextBlock Text="➕ Add Cash" 
                           FontSize="32" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,40"
                           Foreground="{DynamicResource TextBlack}"/>

                <!-- Keypad Input -->
                <controls:KeypadControl Margin="0,0,0,40"
                                        UnitLabel="$"
                                        IsPrefix="True"
                                        Value="{Binding KeypadValue}"/>

                <!-- Action Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Cancel" 
                            Command="{Binding CancelCommand}"
                            Padding="30,15"
                            FontSize="18"
                            FontWeight="SemiBold"
                            Background="{DynamicResource DangerColor}"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Width="180"
                            Height="60"/>
                    
                    <Button Content="Add Cash" 
                            Command="{Binding SubmitCommand}"
                            Padding="30,15"
                            FontSize="18"
                            FontWeight="SemiBold"
                            Background="{DynamicResource SuccessColor}"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Width="180"
                            Height="60"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
