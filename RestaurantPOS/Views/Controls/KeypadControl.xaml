<UserControl x:Class="RestaurantPOS.Views.Controls.KeypadControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:RestaurantPOS.Resources.Converters"
             xmlns:vm="clr-namespace:RestaurantPOS.ViewModels"
             x:Name="Root"
             mc:Ignorable="d">

    <UserControl.Resources>
        <converters:KeypadDisplayConverter x:Key="KeypadDisplayConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Input Display -->
        <Border Grid.Row="0"
                Background="{DynamicResource PaymentInputBackground}"
                CornerRadius="8"
                Padding="20"
                Margin="0,0,0,15"
                Visibility="{Binding ShowDisplay, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibility}}">
            <TextBlock FontSize="48"
                       FontWeight="Bold"
                       Foreground="{DynamicResource BackgroundWhite}"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center">
                <TextBlock.Text>
                    <MultiBinding Converter="{StaticResource KeypadDisplayConverter}">
                        <Binding Path="Value" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                        <Binding Path="UnitLabel" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                        <Binding Path="IsPrefix" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </Border>

        <!-- Number Pad -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Row 1 -->
            <Button Grid.Row="0" Grid.Column="0" Content="7" FontSize="32" Margin="5" Click="Number_Click"/>
            <Button Grid.Row="0" Grid.Column="1" Content="8" FontSize="32" Margin="5" Click="Number_Click"/>
            <Button Grid.Row="0" Grid.Column="2" Content="9" FontSize="32" Margin="5" Click="Number_Click"/>

            <!-- Row 2 -->
            <Button Grid.Row="1" Grid.Column="0" Content="4" FontSize="32" Margin="5" Click="Number_Click"/>
            <Button Grid.Row="1" Grid.Column="1" Content="5" FontSize="32" Margin="5" Click="Number_Click"/>
            <Button Grid.Row="1" Grid.Column="2" Content="6" FontSize="32" Margin="5" Click="Number_Click"/>

            <!-- Row 3 -->
            <Button Grid.Row="2" Grid.Column="0" Content="1" FontSize="32" Margin="5" Click="Number_Click"/>
            <Button Grid.Row="2" Grid.Column="1" Content="2" FontSize="32" Margin="5" Click="Number_Click"/>
            <Button Grid.Row="2" Grid.Column="2" Content="3" FontSize="32" Margin="5" Click="Number_Click"/>

            <!-- Row 4 -->
            <Button Grid.Row="3" Grid.Column="0" Content="." FontSize="32" Margin="5" Click="Decimal_Click"/>
            <Button Grid.Row="3" Grid.Column="1" Content="0" FontSize="32" Margin="5" Click="Number_Click"/>
            <Button Grid.Row="3" Grid.Column="2" Content="C" FontSize="32" Margin="5" Click="Clear_Click"/>
        </Grid>
    </Grid>
</UserControl>
